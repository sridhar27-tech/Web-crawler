A web crawler built with Node.js and TypeScript for collecting programming-related documentation and web content.

The crawler discovers pages from a set of seed URLs, extracts content and metadata, stores crawl results in PostgreSQL, and continuously discovers new pages through link traversal.